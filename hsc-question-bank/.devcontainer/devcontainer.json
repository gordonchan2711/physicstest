{
  "name": "hsc-question-bank",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y poppler-utils && pip install -r backend/requirements.txt",
  "forwardPorts": [5000]
}
